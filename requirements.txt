fastapi>=0.115.0
uvicorn>=0.30.0
aiosqlite>=0.20.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
httpx>=0.27.0
cryptography>=43.0.0
base58>=2.1.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
python-dotenv>=1.0.0
pywebview>=6.0.0
python-multipart>=0.0.9

# KB + CU 新增依赖
chromadb>=0.5.0,<1.0.0  # v2.2.2: PYSEC-2026-311 影响 1.0.0+,限制 <1.0.0
llama-index>=0.11.0
lightrag-hku>=1.0.0
kuzu>=0.4.0
python-docx>=1.1.0
trafilatura>=1.12.0
pyyaml>=6.0
browser-use>=0.1.0
playwright>=1.45.0
# KB parser/retrieval 补充依赖 (v2.1.14)
numpy>=1.26.0
pandas>=2.1.0
openpyxl>=3.1.0
pypdf>=4.0.0
# marker-pdf 为可选依赖 (依赖 PyTorch ~2GB,会导致 Windows WiX 打包失败)
# 如需高级 PDF 解析 (OCR/表格识别),用户可手动 pip install marker-pdf
# PdfParser 在无 marker 时自动降级到 pypdf 文本提取
# marker-pdf>=0.2.0

# v2.2.0 新增依赖 (Phase 2-5: 命令执行 + 代码执行 + LanceDB KB + Browser/CU)
# LanceDB 向量数据库 (Phase 4 知识库,PyO3 依赖 pyarrow)
lancedb>=0.15.0
# 终端命令执行 (Phase 2,跨平台 PTY)
ptyprocess>=0.7.0
pywinpty>=2.0.0; sys_platform == "win32"
# 文档解析补充 (Phase 4 KB doc_parser,PowerPoint 格式)
python-pptx>=0.6.23
# Computer Use (Phase 5,屏幕截图 + GUI 自动化 + Windows a11y 树)
Pillow>=10.0.0
pyautogui>=0.9.54
uiautomation>=2.0.0; sys_platform == "win32"
